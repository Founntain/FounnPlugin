package dev.founntain.founnplugin.recipes;

import org.bukkit.ChatColor;

public class BaseRecipe {
	protected final String prefix = ChatColor.WHITE + "[" + ChatColor.LIGHT_PURPLE + "R" + ChatColor.WHITE + "] " + ChatColor.AQUA;
}
